## Learned User Preferences

- Prefer simple, user-facing labels over technical names (e.g., "Percentile Rank" instead of "percentrank_inc"); avoid Excel-specific or source-format language in UI copy
- Prefer integrating projected/scenario data into existing year dropdowns rather than creating separate standalone analysis sections; keep experimental methodology changes admin-only until validated rather than changing public/shared analyses by default; label ratings by their Star year using app-wide conventions (e.g., frame the 2027 QBP rating as "Stars 2026" since it derives from the 2026 Star Ratings), reserving "QBP" only for the bonus payment/eligibility/dollar impact itself
- Use light blue (#c7d7e8) for table header backgrounds instead of dark navy; avoid blue-on-blue text combinations; readability over brand color fidelity
- PDF exports must mimic the on-screen UI in its rendered (light) theme on portrait 8.5×11 letter pages—never export dark mode; client-facing contract reports should be professional presentation-grade multi-page documents (one report page per PDF page) with polished charts
- Keep search/filter controls compact so sort and filter options stay visible without scrolling
- Use H+R (both H and R prefix) contract population consistently across all analyses, not H-only; parent-org aggregate views should disclose enrollment and reward-factor contract counts when using aggregated chart data
- More recent years should carry more weight in pooled analyses (recency weighting: 2024=1x, 2025=2x, 2026=3x)
- Measures get whole star ratings only (1, 2, 3, 4, 5); never display half stars at the measure level; charts should show the numeric score on or above the bars and display the numeric score alongside the star rating, matching the convention used elsewhere in the app; apply chart value/dollar-impact labels (e.g., estimated QBP swing) consistently across all scenario bars, not just a subset
- Exclude "dropped" contracts (those that exited the market) from year-over-year analyses; they are not meaningful for performance tracking
- Summary card order should be Declined → Held → Improved (left to right); "Improved" belongs on the right
- Display star thresholds in descending order (5-star on top) in tooltips, tables, and charts; in within-band score drilldowns, list higher starting scores first
- Cut point forecast measure detail should surface current month, prior-year same month, prior-year final, and projected year-end together (not redundant duplicate scores); chart legend uses plain year labels without an "Observed" prefix; manual forecast overrides should default from modeled values and visually indicate when edited; full-market cut point projections must stay within historical movement ranges—real market cut points rarely swing several points year over year, so large projected swings indicate a methodology flaw (client-only volatility from small samples is expected and acceptable)

## Learned Workspace Facts

- Next.js app deployed on Vercel; Python deps installed at prebuild via `scripts/generate-percentile-data.sh` using `uv run --with`; Vercel runtime is read-only (only `/tmp` writable), generated files bundled via `outputFileTracingIncludes`; dev server on localhost:3001
- Percentile analysis supports four methods: Percentile Rank, Percentile of Score, Corrected Mid-Rank, and KDE Smoothed; the latter two compensate for integer-score discretization and ties
- CMS Star Ratings data spans 2022-2026 measures with cut points from 2016-2028 (2027/2028 are user forecasts); cut points with forecasts at `data/Stars 2016-2028 Cut Points 12.2025_with_weights.xlsx`; the xlsx npm package parses workbooks server-side
- Cut-point methodology backtest (CMS Backtest mode under `/analysis/band-movement`) uses Ward clustering, 10-fold mean resampling, Tukey, and guardrails for non-CAHPS; P15/P30/P60/P80 percentile thresholds for CAHPS; only Quality Improvement measures excluded; validated against external research replication (~82% exact match, see `docs/cms-clustering-replication-2026-*`); full batch export of all measures: `npm run export:methodology-backtest` writes timestamped CSV/JSON under `data/exports/` (same Actual vs Simulated logic; “Diff” = client simulated minus full market simulated using `data/client-contracts.xlsx`); a collapsible Combined Accuracy / all-measures view (`analyzeCutPointMethodologyOverall()` in `src/lib/band-movement/cut-point-methodology.ts`, API view `methodology-backtest-overall`) pools every 2★–5★ absolute error across all backtestable measures/years into a single combined MAE (full market + client) with by-threshold and per-measure breakdowns, computed deterministically and cached for the process lifetime and fetched on demand
- CAHPS measures (including Annual Flu Vaccine/C03) use a percentile-based relative distribution method for star assignment, not clustering; public CMS data lacks standard errors and reliability estimates, so only the base group percentile thresholds can be replicated
- Contract data uses H and R prefixes (Medicare Advantage); both included in all analyses; client-only backtest filtering supported via `data/client-contracts.xlsx` with `clientOnly=true` API parameter; measure codes change between years (e.g., C04 renamed in 2026), so cross-year matching must use normalized measure names, not code prefixes; the Contract Summary screen (`/`, `/summary`) has a Contract vs Parent Organization view toggle—parent-org mode shows a searchable org dropdown, each contract's CMS Overall/Part C/Part D ratings and enrollment (% of org population, states served), and an aggregate Population by State breakdown via `src/app/api/summary/parent-org/route.ts` reusing the `/api/peer/states` landscape↔enrollment join (latest enrollment period)
- Band movement analysis at `/analysis/band-movement` tracks contract performance migration between star bands year-over-year; fractional band position uses `star + ratio`; edge bands use score scale endpoints (0/100) as synthetic boundaries; Cut Point Impact correlates cohort score change and cut-point change for the same year-to-year step, projects future cut points, and reports both mean and true per-contract median score deltas—median score-change metrics belong on Cut Point Impact, not the main band movement page
- CMS implemented Tukey outlier deletion starting 2024; pre-2024 and post-2024 cut points are not directly comparable; Tukey filtering is skipped when IQR=0 (degenerate case with >50% ties at one score value); 41 of 43 CMS measures have integer-only scores with heavy ties (up to 26% of contracts sharing one value); publicly released scores are whole numbers, limiting exact backtest accuracy; roster accuracy curve and decimal uplift analyses model the impact of sample size and score precision
- Reward factor projection at `/analysis/reward-factor-projection` lets users upload projected scores via CSV, converts to stars using cut points, and computes weighted mean/variance percentile thresholds (65th/85th for mean, 30th/70th for variance); backtested against prior-year published thresholds from `data/mean_thresholds.csv` and `data/variance_thresholds.csv`; supports with/without Quality Improvement measures scenarios and a QI threshold correlation chart/table for official Overall thresholds; Clover/scenario bars use unrounded calculated scores (not official rounded scores as deltas), compare 2026 model deltas against the calculated result selected by the QI hold-harmless rule, recompute scenario reward-factor thresholds from the full H+R MA-PD population after recalculating each contract's mean/variance over remaining measures using PERCENTILE.INC mean 65/85 and variance 30/70, and add CAI after reward factor (2026 Overall FAC rows map to CMS Technical Notes CAI values); the per-contract overview table includes contract name and parent organization from `measure_stars_{year}.json`; projected years 2028/2029 use 2026 data with CMS-announced measure retirements removed (removal definitions in `src/lib/reward-factor/measure-removal-projection.ts`), surfaced as selections in the existing year dropdown, with a per-contract qualification analysis that pulls each contract's most recent scores forward to show who would have qualified for a reward factor in that year; the Clover impact screen (`/analysis/clover-impact`) compares official ratings against measure-removal scenario models at contract and parent-org level (equal-weight vs enrollment-weighted, enrollment from monthly report uploads) and includes a ballpark QBP (quality bonus payment) dollar-impact analysis per scenario—no QBP deltas on official SY26 bars, with driver contracts listed alongside their enrollment; shared duplicate measures are de-duplicated before computing Overall MA-PD scores, keeping the Part C versions and dropping the Part D duplicates (`OVERALL_DEDUP_DROP_CODES = {D02, D03}`: Complaints C28/D02, Members Choosing to Leave C29/D03); the Clover screen supports PNG export of charts and CSV export of tables, and reward-factor contribution is shown with color-coded circles next to each contract; the screen also models CMS's June 17, 2026 voluntary recalculation (following Clover Insurance Co. v. HHS, S.D. Ga.) as the headline `officialRecalc` scenario in `src/lib/clover-impact/scenarios.ts`—recalculating the Stars 2026 rating that drives the 2027 QBP using only Part C HEDIS/CAHPS/HOS measures (`OFFICIAL_RECALC_REMOVED_CODES` removes all Part D D01–D12 plus six named Part C measures C07/C28/C29/C31/C32/C33), applied hold-harmless via `buildQbp2027` so each contract's final rating = max(original rounded, recalc rounded) and no contract is downgraded, with bid-resubmission eligibility flagged when the increase crosses a 3.5/4.0/4.5 benchmark/rebate tier; the contract/parent tables show the raw recalc score for transparency while the hold-harmless final rating drives the QBP/dollar figures, and the per-contract Scenario Measure Scores table shows both removed and kept measures with a Removed/Kept/All filter and status badges, computes a weighted-mean star score for each of the All/Removed/Kept columns (the base mean, not the final rating) plus a "Score buildup" line decomposing the scenario's recalc score as base mean + reward factor + CAI (with a "QI hold-harmless" badge when the without-QI leg wins, which is why a contract's Kept score can differ from its official recalc score), and the Scenario Definitions cards display each scenario's reward-factor thresholds (PERCENTILE.INC mean P65/P85, variance P30/P70) in both With QI and No QI variants; the official recalc keeps the Part C Health Plan Quality Improvement measure (C30) at its published 2026 star value rather than recomputing QI over the surviving post-removal measures—a faithful QI recompute is not possible because public CMS data lacks per-contract standard errors/reliability/denominators and the QI-specific star cut points, and CMS computes QI over its own fixed eligible-measure set independent of the summary; the official recalc applies the published 2026 Part C CAI (Technical Notes Table 15, keyed by Part C FAC) since dropping all Part D makes it a Part C summary rating, while every other scenario keeps the Overall MA-PD CAI (CMS's actual recalc worksheets use an unpublished CAI recomputed for the reduced measure set, so the published Part C CAI is the closest public proxy); the recalc engine is validated against CMS's official per-contract SR calculation worksheet (e.g., `H1225_2026_SR_Calculations`) with base mean matching to 6 decimals; the Clover baseline (original 2026 calculated score) uses the official published reward-factor thresholds via `getOfficialForScenario(2026, "overall_mapd", true)` from `data/mean_thresholds.csv`/`data/variance_thresholds.csv` (falling back to recomputed PERCENTILE.INC), while scenarios still recompute their own thresholds; the Official Stars 2026 Recalculation Impact table lists all contracts (not just improved) with Original Calc Score and Part C CAI columns and asterisks scores that round below CMS's official rating; the proper CMS QI hold-harmless (compute the full final rating both with and without the improvement measures C30/D04 using each leg's official thresholds and take the higher—low QI stars inflate reconstructed variance and kill the reward factor) is applied to the calculated 2026 baseline (`calculated2026HoldHarmless` drives the Original Calc Score column and asterisks, while the pure with-QI score still powers the S26 With QI chart bar) and to all scenarios except the intentionally pure `s26NoQI`, collapsing rounds-below-official from 48 contracts to 4 (two 5.0 high performers with a residual reconstruction gap and two heavy-disaster contracts with un-modeled EUC "higher-of" uplift); disaster-affected contracts get a Disaster % badge (max of 2023/2024 disaster percentages from `summary_rating_2026.json`)
- Tests: `npm run test:percentile-analysis`, `npm run test:band-movement`, `npm run test:plan-preview`; cut point projections for 5-star thresholds capped at 100; PNG/PDF export uses `dom-to-image-more` 3.10.2 (3.7.1 hung indefinitely—3.10.2 adds stylesheet/font/image fetch-settlement and ships types), and the shared renderer (`src/lib/export/dom-image.ts`) mounts the export clone beside the original element so it inherits the local CSS cascade/theme and resolves the background from the nearest non-transparent ancestor, so each screen exports in the theme it actually renders in; `exportPagesToPdf` in `src/lib/export/pdf.ts` renders fixed-size pages one-per-PDF-page for multi-page letter reports
- Plan Preview 1 module (`/admin/plan-preview`, admin-gated like forecast; PP1 is the August HPMS release where plans see measure scores without star ratings—PP2 adds the stars): migration 016 creates the `plan_preview_*` tables (upload batches, accrued measure scores, CAI) keyed by stars year + contract + measure so re-uploads replace rows and the pool accrues as plans' files arrive (must be applied to prod Supabase before deploy); `src/lib/plan-preview/workbook.ts` auto-detects the measure vs CAI file formats (samples in `data/2027/`), converts percent/decimal strings, preserves sentinel statuses, and resolves names via canonical measure matching with a fallback to the file's own measure code (D08–D10 shorthand names); the selected Star year is authoritative over the year in the file title (mismatch stored and warned); `predictions.ts` overlays accrued contracts onto the latest published market (Anchored Full Market) via `analyzeCutPointMethodologyForecast`—predicted cut points correctly sit at prior-year official values until real market movement accrues, S-prefix PDP contracts excluded; `final-scores.ts` reuses the validated clover-impact machinery with QI hold-harmless (PP1 files carry no QI scores, so a with-QI leg carries each contract's baseline published QI stars forward and the higher leg wins) plus four scenario overlays (all measures, Stars 2028/2029 removals, Clover-style recalc using the uploaded Part C CAI); results are cached per stars year in `run-cache.ts`, shared by the predictions and report APIs; UI uses the FEP blue theme from the user's local `FEP Monitor` repo (fepmonitor.com) ported into `src/app/admin/plan-preview/fep-theme.css` scoped under a `.fep` wrapper with Newsreader/Hanken Grotesk fonts loaded in the section layout; the multi-page contract report at `/admin/plan-preview/report?starsYear=&contractId=` renders four fixed 8.5×11 pages (overall prediction with score buildup, domain breakdown using `ma_measures` domain assignments, year-over-year vs published ratings, scenario analysis) exported via `exportPagesToPdf`
- Cut point forecast (`/admin/forecast`; Forecast tab on `/analysis/band-movement` reads approved runs): glidepath treats CAHPS/HOS as prior-year final unless manually overridden; HEDIS/pharmacy use ±2/±1 guardrails anchored to **prior-year final** when available (not last observed), with year-end projection driven by prior-year close adjusted for the current-vs-prior same-month gap when modeled; projections are computed at import and stored—**Re-run Projections** or re-import refreshes values after logic changes; approved forecast runs can be applied as projected client data in CMS Backtest and Cut Point Impact; the public Forecast tab falls back to the latest historical market baseline in `full_market` when no approved run exists, while `client_only` requires approved projected client scores; full-market methodology no longer uses uniform market score shift—admin compares Anchored Full Market (replace/append projected client scores, leave non-client market unchanged), Client-Informed (matched client movement shrunk by sample size/representativeness and capped), and Client Only; historical plausibility should be compact scenario-level cards, not repeated per-threshold warning walls; measure approvals are saved per run/year and measure (`forecast_measure_approvals`), cleared when an approved measure is edited, and a missing approvals table should not block projection data load; confidence labels use `>=0.75` high, `>=0.45` medium, otherwise low; dummy/test contracts are filtered out via a repeating-digit pattern (H1111…H9999/H0000, regex `/^H(\d)\1{3}$/`) plus a requirement that eligible forecast contracts tie to a real parent org in the published star universe (`isEligibleForecastContract` in `src/lib/cutpoint-forecast/analysis.ts`); HL codes live only in the `forecast_*` Supabase tables, compact uploads use HL codes or equivalent EQ codes as the required join key (EQ numbers map to the same measures as HL codes), non-CAHPS uses stars year directly, CAHPS reporting year + 1 = stars year, letter-suffix contracts and zero values are excluded, run selections are keyed/shared by stars year, and SY26 is locked while SY27/SY28 are editable; admin routes are gated by an `approved` row in `user_approvals` (`src/lib/admin/require-approved-admin.ts`) and the forecast admin depends on migrations 013–015 plus the `forecast_*`/`user_approvals` tables existing in the target Supabase, so admin pages/API routes must be committed and those migrations applied in prod or they 404/error
